# ServerProperties
server:
  port: 8080
  servlet:
    context-path: /api

# logger logback-spring.xml中配置了
#logging:
#  level.com.swking: debug
#  file:
#    path: C:\CodeSpace\JavaSpace\blog_community_server\log
#    name: blog_community.log

spring:
  # ThymeleafProperties
  thymeleaf:
    cache: false
  # DataSourceProperties
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog_community?characterEncoding=utf-8&useSSL=false&serverTimezone=Hongkong
    username: root
    password: swking0917
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 15
      minimum-idle: 5
      idle-timeout: 30000
  # MailProperties
  mail:
    host: smtp.qq.com
    port: 465
    username: swkings@qq.com
    password: erupjzdoswtfjjaa
    protocol: smtps
    properties:
      mail:
        smtp:
          ssl:
            enable: true
  # RedisProperties
  redis:
    # 可以自己选择
    database: 0
    host: localhost
    port: 6379

# MybatisProperties
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.swking.entity
  configuration:
    use-generated-keys: true
    map-underscore-to-camel-case: true

# 自定义参数
blogCommunity:
  path:
    domain: http://localhost:8080
    upload: C:/CodeSpace/JavaSpace/blog_community_server/ext/upload
  client:
    domain: http://localhost:3001
  enable-redis: true